<% if @cls == 'ProjectGood' %> 
  filesappModal.setData('ProjectGood', <%= for_vue(@file, [:owner_name]) %>);
<% else %>
  filesapp.setData('<%= @cls %>', <%= for_vue(@file, [:owner_name]) %>);
<% end %>