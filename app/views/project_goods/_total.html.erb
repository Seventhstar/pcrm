<% if @goods.nil? %>
  <% get_goods(@prj_good.project_id,pgt) %>
<% end %>
<tr id= "total_<%= @cur_id %>" class="total <%= @goods.count>0 || opened ? '' : 'hidden' %>">
  <%= render 'project_goods/total_body' %>    
</tr>
