<% if first_table == false %>
</tbody></table></div>
<% end %>
<% first_table = false %>
<div class="panel panel-default">
<% if !heading.empty? %>
  <div class="panel-heading table-heading head month"> <%= heading %></div>
<% end %>
<table class="tleads projects">
  <thead>
    <tr>
      <th width="40px">Номер</th>
      <th>Адрес</th>
      <th width='140px'>Вид проекта</th>
      <th width='150px'>Заказчик</th>
      <% if is_admin? %><th width='120px'>Исполнитель</th><% end %>
      <th width="85px">Начало</th>
      <th width="85px">Конец / Продления</th>
      <th width="37px">пл.</br> 1/м2</th>
      <th class="td_right" width="70px">Стоимость<%= is_admin? ? '' : ' для дизайнера' %></th>
      <% if is_admin? %>
        <th></th>
      <% end %>


    </tr>
  </thead>

  <tbody>
