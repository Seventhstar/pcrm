<script>
  Vue.component('v-select', VueSelect.VueSelect)
  
  let ptype = new Vue({
    el: '.prj_type',
    data: <%= fill_vue_data(@project,
     {
      model: 'project',
      
      list_values: 'project_type city client executor visualer project_type style pstatus client',
      lists: "project_types+simple cities executors visualers styles pstatuses clients"      
    }, 'page1') %>,

    created() {
      // console.log('created', 'project_type', this.project_type);
    },

    watch: {
      project_type(){
        if (footages.project_type_id>0 && this.project_type != undefined){

          footages.project_type_id = this.project_type.value
          footages.tarifSelect()
          footages.projectTypeSelect()
        }
        // console.log("this.project_type", this.project_type)
        // if (this.project_type === undefined){
        //   this.complexProjectType = false
        // } else {
        //   this.complexProjectType = !this.project_type.simple;
        // }
      }
    }

  });

</script>