Пользователь "<%= find_version_author_name(@version) %>" изменил лид:

<% puts @version.to_s %>

Изменения в лиде <%= @lead.info  %>:

  <%- @history.each do |version_fields| %>
  <%- version = version_fields[1] %>
	<%- version['description'].each do |descr| %>
	   <%= raw(descr) %>
        <% end %>
  <% end %>