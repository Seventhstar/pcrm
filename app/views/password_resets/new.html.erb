<div class="page-wrapper">
  <div class="js-notes has-note">
    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, id: "flash_#{name}", class: "alert fade-in in flash_#{name}" %>
    <% end %>
    <a class="close note-close" href="#"></a>
  </div>

<div class="box_cent form_c auth_form" style="top: 58.5px;">
<div class="hl hl_a bd">Восстановление пароля</div>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(:password_reset, url: password_resets_path) do |f| %>      
      <%= f.label 'E-mail:' %>
      <%= f.email_field :email, class: 'form-control' %>
      <%= f.submit 'Отправить', class: 'sub btn_a password_resets right' %></td></tr>
    <% end %>
  </div>
</div>
</div>