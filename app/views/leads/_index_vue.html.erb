<script>

  let app = new Vue({
    el: '#div_tleads', 
    mixins: [m_created, m_index, mCheck],
    data: <%= fill_vue_data(nil, {
      filterItems: ['priority', 'user'],
      groupBys: 'start_date status_date ic_user user status_id',
      searchFileds: ['info'],
      sortKey: 'start_date',
      reverse: true,
      currentIndex: -1,
      currentMonth: -1,
      confirmModal: false,
      groupName: '',
      groupHeaders: [],
      showTotal: false,
      filter: [],
      grouped: [],
      filteredData: [],
      list_values: '',
      lists: 'mainList:raw@json_leads',
      columns: @columns
      

    }) %>,

  })
</script>