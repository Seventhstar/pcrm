<script>

  let app = new Vue({
    el: '#div_tleads', 
    mixins: [m_created, m_index, mCheck],
    data: <%= fill_vue_data(nil, {
      filterItems: ['priority', 'user'],
      groupBy: 'month',
      groupBys: 'month:Приоритету status_month:Дате_статуса ic_user:Ответственным user:Создателям status_id:Статусам',
      searchFileds: ['info'],
      sortKey: 'start_date',
      tooltips: 'info:70 fio:priority',
      amounts: 'footage',
      reverse: true,
      currentIndex: -1,
      currentMonth: -1,
      confirmModal: false,
      groupName: 'month',
      groupHeaders: [],
      showTotal: false,
      readyToChange: false,
      filter: [],
      grouped: [],
      filteredData: [],
      list_values: [],
      lists: 'mainList:raw@json_data',
      params: @params,
      onlyActual: @only_actual,
      columns: @columns
    }) %>,

  })
</script>