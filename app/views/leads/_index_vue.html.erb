<script>

  let app = new Vue({
    el: '#div_tleads', 
    mixins: [m_created, m_index, mCheck],
    data: <%= fill_vue_data(nil, {
      filterItems: ['priority', 'user'],
      groupBys: ['start_date', 'status_date', 'ic_users.name', 'users.name', 'status_id'],

      sortKey: 'start_date',
      reverse: true,
      currentIndex: -1,
      currentMonth: -1,
      confirmModal: false,
      groupHeaders: [],
      showTotal: false,
      filter: [],
      grouped: [],
      filteredData: [],
      list_values: '',
      lists: 'mainList:raw@json_leads',
      columns: 'status status_date fio phone info footage start_date'
      

    }) %>,

    created(){
      // import {store} from "v-store"
      this.fGroup()
      console.log('created', store.state.searchText);
    },
    
    computed: {
      count() {
        return store.state.count;
      },

      search () {
        return store.state.searchText;
      }
    },

    methods: {
      methodName () {
        
      }
    }

  })
</script>