Пользователь "<%= find_version_author_name(@version) %>" изменил задачу:

Изменения в задаче <%= @dev.name  %>:

  <%- @history.each do |version_fields| %>
  <%- version = version_fields[1] %>
	<%- version['description'].each do |descr| %>
	   <%= raw(descr) %>
        <% end %>
  <% end %>

  <%= link_to 'Перейти к задаче', [:edit, @dev] %>