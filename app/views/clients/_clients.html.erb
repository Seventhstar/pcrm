<div class="panel panel-default">
<table class="tleads">
  <thead>
    <tr>
      <th>ФИО</th>
      <th>Адрес</th>
      <th>Телефон</th>
      <th>E-mail</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @clients.each do |client| %>
      <tr>
        <td><%= client.name %></td>
        <td><%= client.address %></td>
        <td><%= client.phone %></td>
        <td><%= client.email %></td>
        <%= tool_icons(client,{icons:'edit,show'}) %>
      </tr>
    <% end %>
  </tbody>
</table>