<script>
  let app = new Vue({
    el: '.clients_index', 
    mixins: [m_created, m_index, mCheck],
    data: <%= fill_vue_data(nil, {
      searchFileds: ['name', 'address'],
      sortKey: 'start_date',
      tooltips: 'info:70 fio:priority',
      amounts: 'footage',
      menu_items: 'Проекты:projects', 
      params: @params,
      onlyActual: @only_actual,
      columns: @columns
    }, nil, 'vue_index') %>,

  })
</script>
<!--       // filterItems: ['reason', 'user'],
      // groupBys: 'month:Месяцам user:Сотрудникам reason_id:Причинам',
 -->